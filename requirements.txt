# Production dependencies for Render / gunicorn.
# DO NOT add pandas / numpy / openpyxl here - they are only needed for the
# `python manage.py import_excel` command at local. Including them here would
# OOM the Render free tier (512 MB RAM build).
# For local development with import_excel, use: pip install -r requirements-dev.txt
#
# setuptools is required because drf-yasg 1.21.7 still imports pkg_resources,
# and Python 3.12+ no longer bundles setuptools in a fresh venv.
# Must be pinned < 81: setuptools 81+ removed pkg_resources from the wheel.

setuptools<81
Django==5.0.6
djangorestframework==3.15.1
drf-yasg==1.21.7
gunicorn==23.0.0
whitenoise==6.7.0
